# Banner asset

The shared PNG is stored at `../fusion_addin/VersatzebenenTool/Resources/banner.png` and referenced by both READMEs and the Fusion dialog. Native size: 2172 × 724 px. The dialog scales the image to 420 × 140 logical pixels through `ImageCommandInput.scaleFactor`; the installer includes it automatically.

Created with the built-in ImageGen tool. No version number or translated tagline is embedded, so the asset can be reused across languages and releases.

## Generation prompt

Use case: ads-marketing. Asset: wide banner for the GitHub README and native command dialog of the Autodesk Fusion add-in VersatzebenenTool. Create a polished restrained CAD engineering illustration, wide 3:1 aspect ratio. Show five equally spaced translucent parallel construction planes in isometric view, fine technical grid lines and a simple sketch contour, conveying precise offset-plane creation. Dark graphite background, luminous restrained teal and warm amber geometry accents, crisp clean technical aesthetic. Exact text: "VersatzebenenTool", large exceptionally legible modern sans-serif, generously spaced from illustration, all content inside generous safe margins. No other text, no version number, no Autodesk logo, no watermark, no UI chrome. Must remain legible in a 420-pixel-wide dialog. Output PNG.
